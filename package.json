{
  "name": "groovify",
  "version": "1.0.0",
  "description": "Project created to enhance knowledge of Authorization and Advanced REST API. Basis of project: Authenticate Client, Fetch user data and listening history to create visual representation of user listening activity. Web/Server Based.",
  "main": "Server/Server.js",
  "scripts": {
    "build": "npm install && npm install --prefix Client && npm run build --prefix Client",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node Server/Server.js",
    "pid": "lsof -i :3000"
  },
  "author": "Jxdn",
  "repository": "https://github.com/ItsHisoka17/Moodify",
  "license": "MIT",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2"
  }
}
